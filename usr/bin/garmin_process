#!/usr/bin/env bash

# sudo zypper install python2-{pip,usb} python-gdbm
# sudo pip2 install ant-downloader
# sudo pip install garmin-updloader==1.0.5

# sudo usermod -a -G dialout $(whoami)

ant-downloader &&
cd .antd/0xe2854cb4/tcx/ &&

PYTHONPATH="$HOME/usr/lib/python2.7/site-packages/" gupload -u michaud.sonny@gmail.com -p $(pass internet/connect.garmin.com) $(ls | tail -n 1) &&
cd -
